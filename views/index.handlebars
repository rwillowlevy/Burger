<div class="columns is-centered">
    <div class="column is-one-third ">
    <h1 class="has-text-centered is-size-1">Eat Your Burger!</h1>
    <figure class="image container">
      <img src="https://cdn.clipart.email/6baf1b13de4344f76e75c5c96ede5612_cartoon-hamburger-png-cheese-burger-clip-art-400x322-png-_820-402.jpeg" alt="Burger">
    </figure>
  </div>
</div>

<div class="columns is-4">
  <div class="column">
    <h2 class="is-size-2 has-text-centered">Not Eaten!</h2>

    <ul>
      {{#each burgers}}
        {{#unless devoured}}
            {{> burgers/burger-block devoured=false}}
        {{/unless}}
      {{/each}}
    </ul>
  </div>

  <div class="column">
    <h2 class="is-size-2 has-text-centered">Add a Burger</h2>
    <form class="create-form">

    <div class="form-group">
      <label for="burger">Burger Name:</label>
      <input type="text" id="burger" name="burger_name">
    </div>

    <div class="form-group">
      <label for="devoured">Devoured?</label><br>
      <input type="radio" name="devoured" value="1" checked> Devoured!<br>
      <input type="radio" name="devoured" value="0"> Not Devoured
    </div>

    <button type="submit" class="button is-primary">Add Burger</button>
   </form>

  </div>


  <div class="column">
    <h2 class="is-size-2 has-text-centered">Devoured!</h2>

    <ul>
      {{#each burgers}}
        {{#if devoured}}
            {{> burgers/burger-block devoured=true}}
        {{/if}}
      {{/each}}
    </ul>
  </div>
</div>